# Code Vault
A simple account management system with a free tier and paid upgrade.

## Usage
1. Create an account using `AccountManager.create_account`.
2. Create escrows using `Account.create_escrow`.
3. Upgrade to a paid tier using `AccountManager.upgrade_account`.
